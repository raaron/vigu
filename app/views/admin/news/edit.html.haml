- provide(:title, admin_title(t(:news)))
- provide(:normal_view_link, news_path)

%h1= "News:"
= form_for @paragraph, :url => admin_news_path(@paragraph), :html => { :multipart => true } do |f|
  = paragraph_form(f)
  = f.submit(t(:save))
